# 体素旋转条纹修复进展

## 本轮完成

- 回退亚体素哈希抖动：它会让相邻完整体素发生重叠/错位，带来深度竞争和频闪。
- 回退 `fwidth(input.posOS)` 远距离细节自适应：它会随旋转角度和 MSAA 子样本变化，造成模糊与亮度频闪。
- 保留上一版相对稳定的屏幕空间哈希抖动，强度恢复为 7%。
- Unity 日志尾部未发现新的 C# 或 Shader 编译错误。

## 当前配置

- URP RenderScale: 1
- URP MSAA: 2x
- 主光阴影支持：关闭（排查期间关闭，非最终结论）
- 体素绘制：ScriptableRendererFeature + 静态注册表，走 URP 管线内的 CommandBuffer
- VoxelLit：Half-Lambert、屏幕哈希抖动 0.07、严格整数体素网格

## 已回退的实验项

- 亚体素位置抖动：会让相邻体素重叠/错位，产生深度竞争和频闪。
- `fwidth(input.posOS)` 远距离 LOD 淡出：随旋转角度和 MSAA 子样本变化，导致模糊与亮度跳变。

## 后续

- 在 Unity Play 模式下远距离连续旋转验证。
- 若仍有残留，不要再修改烘焙数据；`gridPos` 是射线检测、破坏和 GPU 索引的整数坐标契约，在烘焙侧写入随机偏移会造成缝隙、重叠和碰撞/射线不一致。
- 只考虑调整稳定的屏幕空间抖动强度，避免使用任何随视角变化的着色调制。

