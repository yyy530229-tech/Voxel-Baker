# 体素旋转条纹修复进展

## 本轮完成
- 回退亚体素哈希抖动：它会让相邻完整体素发生重叠/错位，带来深度竞争和频闪。
- 回退 `fwidth(input.posOS)` 远距离细节自适应：它会随旋转角度和 MSAA 子样本变化，造成模糊与亮度频闪。
- 保留上一版相对稳定的屏幕空间哈希抖动，强度恢复为 7%。
- Unity 日志尾部未发现新的 C# 或 Shader 编译错误。

## 当前配置
- URP RenderScale: 1
- URP MSAA: 2x
- VoxelLit: Half-Lambert、屏幕哈希抖动、亚体素确定性抖动、距离自适应面朝向/AO

## 后续
- 在 Unity Play 模式下远距离连续旋转验证。
- 若仍有残留，优先只调 `detailKeep` 的远距离衰减曲线，不再修改烘焙数据；`gridPos` 是射线检测、破坏和 GPU 索引的整数坐标契约，直接在烘焙侧写入随机偏移会造成缝隙、重叠和碰撞/射线不一致。
